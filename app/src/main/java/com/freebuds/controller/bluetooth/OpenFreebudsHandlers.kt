package com.freebuds.controller.bluetooth

/**
 * Huawei handlers are split by capability into the neighbouring feature files.  This file is
 * retained as the stable source path for older references; the public handler class names and
 * the HuaweiDeviceHandler compatibility boundary are unchanged.
 */
